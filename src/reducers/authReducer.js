/* eslint-disable import/prefer-default-export */

const initialState = {
  user: '',
};

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

